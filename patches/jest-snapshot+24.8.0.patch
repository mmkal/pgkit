diff --git a/node_modules/jest-snapshot/build/inline_snapshots.js b/node_modules/jest-snapshot/build/inline_snapshots.js
index d6cc2d9..af207b3 100644
--- a/node_modules/jest-snapshot/build/inline_snapshots.js
+++ b/node_modules/jest-snapshot/build/inline_snapshots.js
@@ -155,6 +155,13 @@ const groupSnapshotsByFile = groupSnapshotsBy(({frame: {file}}) => file);
 
 const indent = (snapshot, numIndents, indentation) => {
   const lines = snapshot.split('\n');
+  // Prevent re-identation of inline snapshots.
+  if (
+    lines.length >= 2 &&
+    lines[1].startsWith(indentation.repeat(numIndents + 1))
+  ) {
+    return snapshot;
+  }
   return lines
     .map((line, index) => {
       if (index === 0) {
