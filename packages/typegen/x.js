// const childProcess = require('child_process')

// childProcess.execSync(`echo dist | xargs ls`, {stdio: 'inherit'})
const execa = require('execa')

const main = async () => {
  // const cs = require('cross-spawn')
  // console.log(111, cs._parse('abc', ['def', 'ghi']))
  // const foo = await execa('echo', ['dist', '|', 'xargs', 'ls'])
  // console.log({foo})
}

main()
